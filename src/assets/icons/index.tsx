const JS = () => {
  return (
    <i
      style={{ fontSize: '3rem' }}
      className="devicon-javascript-plain colored"
    />
  );
};

const TS = () => {
  return (
    <i
      style={{ fontSize: '3rem' }}
      className="devicon-typescript-plain colored"
    />
  );
};

const Node = () => {
  return (
    <i
      style={{ fontSize: '3rem' }}
      className="devicon-nodejs-plain-wordmark colored"
    />
  );
};

const Express = () => {
  return (
    <i
      style={{ fontSize: '3rem' }}
      className="devicon-express-original colored"
    />
  );
};

const React = () => {
  return (
    <i
      style={{ fontSize: '3rem' }}
      className="devicon-react-original colored"
    />
  );
};

const Redux = () => {
  return (
    <i
      style={{ fontSize: '3rem' }}
      className="devicon-redux-original colored"
    />
  );
};

const PostgreSQL = () => {
  return (
    <i
      style={{ fontSize: '3rem' }}
      className="devicon-postgresql-plain colored"
    />
  );
};

const MySQL = () => {
  return (
    <i
      style={{ fontSize: '3rem' }}
      className="devicon-mysql-original colored"
    />
  );
};

const Github = () => {
  return (
    <i
      style={{ fontSize: '3rem' }}
      className="devicon-github-original colored"
    />
  );
};

const Git = () => {
  return (
    <i style={{ fontSize: '3rem' }} className="devicon-git-plain colored" />
  );
};

const GraphQL = () => {
  return (
    <i style={{ fontSize: '3rem' }} className="devicon-graphql-plain colored" />
  );
};

const icons = [
  JS,
  TS,
  Node,
  Express,
  React,
  Redux,
  PostgreSQL,
  MySQL,
  Github,
  Git,
  GraphQL,
];

export default icons;
